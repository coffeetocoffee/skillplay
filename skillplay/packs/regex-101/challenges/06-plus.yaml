id: regex-plus-01
skill: regex
title: One or more with +
topic: quantifier
difficulty: 2
xp: 15
type: regex_build
prompt: |
  Write a pattern that matches a run of one or more digits (e.g. "42").
context: {}
answer:
  value: '\d+'
validation:
  mode: regex_tester
  must_match: ["42", "price 100 dollars"]
  must_not_match: ["no numbers", ""]
hints:
  - "`+` means one or more of the preceding token."
explanation: |
  `\d+` matches one or more consecutive digits.
